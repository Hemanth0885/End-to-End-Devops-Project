apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: Go-web-app
spec:
  ingressClassName: nginx
  rules:
  - host: Go-web-app.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: Go-web-app
            port:
              number: 80
